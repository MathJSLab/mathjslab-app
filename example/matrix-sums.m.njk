---json
{
    "comments": {
        "title": {
            "en": "Sum matrix elements by dimension",
            "es": "Sumar elementos de matriz por dimensión",
            "pt": "Somar elementos de matriz por dimensão"
        },
        "matrix": {
            "en": "sample matrix",
            "es": "matriz de muestra",
            "pt": "matriz de amostra"
        },
        "sums": {
            "en": "column sums, row sums, and total sum",
            "es": "sumas por columna, sumas por fila y suma total",
            "pt": "somas por coluna, somas por linha e soma total"
        }
    }
}
---
% {{ comments.title[lang] }}
clear
% {{ comments.matrix[lang] }}
A = [1, 3, 2; 4, 2, 5; 6, 1, 4]
% {{ comments.sums[lang] }}
column_sums = sum(A)
row_sums = sum(A, 2)
total_sum = sum(column_sums)
